# Example .env file for docker-compose.yml
# Copy this to .env and fill in your actual values.

# Database Configuration
DB_NAME=quanlydiem_db
DB_USER=quanlydiem_user
DB_PASSWORD=your_strong_db_password

# API Configuration (if any additional variables are needed)
# API_SECRET_KEY=your_api_secret_key